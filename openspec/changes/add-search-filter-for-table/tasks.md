## 1. UI Updates

- [x] 1.1 Add search input HTML element above the employee table in `frontend/index.html`.
- [x] 1.2 Update `frontend/index.css` to style the search input (e.g., width, padding, background, border, margins) to match the dashboard's dark theme.

## 2. Logic Implementation

- [x] 2.1 Add an event listener to the search input in `frontend/app.js` for the `input` event.
- [x] 2.2 Implement filtering logic in `frontend/app.js` that iterates over the displayed table rows and toggles their visibility based on whether the row's text content matches the search string case-insensitively.
- [x] 2.3 Verify that clearing the search input (empty string) resets all rows to be visible.
